simple tool to get info about lines in text file
functions:
list unique lines (~distinct)
list not-uniqe lines
count occurences of lines (with sort)


windows version
it is for practicing, i am sure that these tasks can be done with tools like grep, awk, etc.,,
